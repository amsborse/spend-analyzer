# Spend Analyzer — development environment
# Usage: docker compose up
# TODO: Add services for API, PostgreSQL, and web when Docker setup is implemented.

version: "3.9"
services:
  # api:
  #   build: ./backend
  #   ports: ["8000:8000"]
  #   environment:
  #     DATABASE_URL: postgresql://user:pass@db:5432/spend
  #   depends_on: [db]
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: pass
  #     POSTGRES_DB: spend
  #   volumes: [pgdata:/var/lib/postgresql/data]
  # web:
  #   build: ./frontend
  #   ports: ["5173:5173"]
  #   depends_on: [api]
# volumes:
#   pgdata:
